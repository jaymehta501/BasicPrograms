package core.java;

public class WaitSleepDifference {
	/**
	 * Sleep is temporary pause for
	 * certain period of time on current
	 * running thread
	 * 
	 * Wait is for conditional wait and
	 * it releases lock which can be
	 * acquired by some different thread
	 * to change the condition on
	 * waiting
	 */
}
