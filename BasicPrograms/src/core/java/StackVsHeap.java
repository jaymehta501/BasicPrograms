package core.java;

/**
 * Stack and heap are different memory
 * areas in the JVM and they are used
 * for different purposes. The stack is
 * used to hold method frames and local
 * variables while objects are always
 * allocated memory from the heap. The
 * stack is usually much smaller than
 * heap memory and also didn't shared
 * between multiple threads, but heap is
 * shared among all threads in JVM
 * 
 * 
 *
 */
public class StackVsHeap {

}
